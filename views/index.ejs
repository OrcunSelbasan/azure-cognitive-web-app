<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <ul>
      <% for(var i=0; i < translations.length; i++) { %>
        <li><b>uid: </b><%= translations[i].uid %> | <b>text: </b><%= translations[i].text %> |<b>To: </b><%= translations[i].to %> |</li>
      <% } %>
      <br>
    </ul>

    <form action="/" method="POST">
      <label for="text"></label>
      <textarea name="text" id="" cols="30" rows="10" required></textarea>
      <br />
      <label for="to">Translate to: </label>
      <select name="to" id="" required>
        <option value="en">English</option>
        <option value="de">German</option>
      </select>
      <button type="submit">Translate</button>
    </form>

    <p><b>Translated text: </b> <%= translationBody %></p>
  </body>
</html>
